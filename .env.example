# Used by docker-compose.yml for local Postgres only — copy to .env and fill
# in real values. Never commit the real .env (already gitignored).
POSTGRES_DB=contactform
POSTGRES_USER=contactform
POSTGRES_PASSWORD=
POSTGRES_HOST_PORT=5433
