apiVersion: v1
kind: Service
metadata:
  name: contactform
  namespace: {{ app_namespace }}
spec:
  type: ClusterIP
  selector:
    app: contactform
  ports:
    - port: 80
      targetPort: {{ app_port }}